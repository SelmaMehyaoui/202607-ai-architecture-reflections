# Security fixtures

Every value here is deliberately fake. Permission probes must use these files
and disposable temporary directories only. `allowed/writable/` is created by
the probe when needed and is not a store for results.
